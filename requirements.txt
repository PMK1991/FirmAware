# Streamlit Community Cloud installs from requirements.txt and ignores
# pyproject.toml, so this file only points the deploy at the "app" extra that
# already pins every version. Nothing is restated here, because a second copy
# of the pins is a second thing to forget to bump.
#
# The app extra leaves numpy to a range rather than an exact pin: Community
# Cloud chooses the interpreter, and an exact 1.26.4 made the deploy try to
# compile numpy from source on any version it had no wheels for, which is what
# "Error installing requirements" was. Training still pins it exactly.
.[app]
